# Copy to `.env` and fill in. `.env` is gitignored.

# Ollama Cloud API key — required when using https://ollama.com as the endpoint.
# Get one at https://ollama.com after signing up.
# Leave blank for local Ollama at http://localhost:11434.
OLLAMA_API_KEY=
